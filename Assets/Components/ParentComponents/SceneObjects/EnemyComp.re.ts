import * as RE from 'rogue-engine';
import SceneObject from '../SceneObject.re';

export default class EnemyComp extends SceneObject {

  start() {
    super.start();
  }

  update() {

  }
}

RE.registerComponent(EnemyComp);
